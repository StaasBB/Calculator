/* ==========================================================================
   ОРИГИНАЛЬНЫЕ ПЕРЕМЕННЫЕ ЦВЕТОВ RFAB (СТАБИЛЬНЫЙ ВАРИАНТ)
   ========================================================================== */
:root {
    --red: #ea9999;
    --light-red: #f4cccc;
    --green: #b6d7a8;
    --light-green: #d9ead3;
    --blue: #9fc4e9;
    --light-blue: #cfe2f3;
    --yellow: #ffe599;
    --light-yellow: #fff2cc;
    --purple: #b4a7d6;
    --light-purple: #d9d2e9;
}

/* ==========================================================================
   ОПТИМИЗАЦИЯ: Подтягиваем оригинальные шрифты напрямую из базы знаний RFAB
   ========================================================================== */
@font-face {
    font-family: 'Oswald';
    src: url('https://skyrimrequiem.ru') format('truetype');
    font-weight: 300;
    font-style: normal;
}

@font-face {
    font-family: 'Oswald';
    src: url('https://skyrimrequiem.ru') format('truetype');
    font-weight: 400;
    font-style: normal;
}

@font-face {
    font-family: 'Oswald';
    src: url('https://skyrimrequiem.ru') format('truetype');
    font-weight: 600;
    font-style: normal;
}



html, body {
    height: 100%;
}

body {
    font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
    margin: 0px;
    overflow: hidden; /* Выключаем скролл для калькуляторной платформы */
    background-image: url("../assets/background.jpg");
    background-size: cover;
    background-repeat: no-repeat;
    background-position: center center;
    background-attachment: fixed;
    display: flex;
    flex-direction: column;
}

main {
    flex: 1;
}

a {
    color: black;
}

/* ==========================================================================
   ОРИГИНАЛЬНЫЙ HEADER RFAB (МНОГОСЛОЙНЫЙ)
   ========================================================================== */
header {
    background: transparent;
    width: 100%;
    height: 60px;
    display: flex;
    position: fixed;
    top: 0;
    left: 0;
    justify-content: center;
    align-items: center;
    z-index: 100;
}

header::before {
    content: "";
    position: absolute;
    inset: 0;
    background-color: rgba(0, 0, 0, 0.8);
    backdrop-filter: blur(8px);
    -webkit-backdrop-filter: blur(8px);
    z-index: -2;
}

.header-img-a {
    height: 100%;
    display: flex;
    align-items: center;
}

.header-img {
    width: auto;
    height: 30px;
    transition: 0.2s all ease;
}

.header-social-platforms {
    display: flex;
    gap: 20px;
}

.header-social-platforms .header-img {
    display: flex;
    gap: 20px;
    height: 28px;
}

.header-separator-200 {
    width: 200px;
}

.header-links {
    display: flex;
    height: 100%;
    align-items: center;
}

.header-link {
    font-family: 'Oswald', sans-serif;
    font-weight: 400; /* ИСПРАВЛЕНО: Буквы станут аккуратными и тонкими, как на оф. сайте */
    font-size: 14px;
    position: relative;
    color: white;
    padding: 18px 24px;
    height: 100%;
    display: flex;
    align-items: center;
    text-decoration: none;
    overflow: hidden;
    box-sizing: border-box;
}

.header-link.double-row {
    flex-direction: column;
    justify-content: center;
    text-align: center;
    line-height: 15px;
    font-size: 12.5px;
}

.header-link::after {
    content: "";
    position: absolute;
    inset: 0;
    background: linear-gradient(rgb(113, 90, 36), transparent);
    opacity: 0;
    transition: opacity 0.5s ease;
    z-index: -1;
}

.header-link:hover::after {
    opacity: 1;
}

.header-link.active {
    color: #f6b36a;
}

.header-img:hover {
    filter: brightness(0.6);
}

/* ==========================================================================
   ОФИЦИАЛЬНАЯ ПАНЕЛЬ UTILITIES BAR
   ========================================================================== */
.tab-bar.app-utilities-bar {
    background-color: transparent;
    display: flex;
    justify-content: center;
    padding: 0px;
    gap: 0px;
    position: fixed;
    top: 60px;
    z-index: 99;
    width: 100%;
    height: 45px;
    box-sizing: border-box;
    border-bottom: 1px solid rgba(255, 255, 255, 0.05);
}

.tab-bar.app-utilities-bar::before {
    content: "";
    position: absolute;
    inset: 0;
    background-color: rgba(0, 0, 0, 0.8);
    backdrop-filter: blur(8px);
    -webkit-backdrop-filter: blur(8px);
    z-index: -2;
}

.tab-bar.app-utilities-bar .tab-button {
    font-family: 'Oswald', sans-serif;
    text-transform: uppercase;
    letter-spacing: 1px;
    position: relative;
    padding: 0px 24px;
    height: 100%;
    display: flex;
    align-items: center;
    font-size: 14px;
    color: #ddd;
    background: transparent;
    border: none;
    cursor: pointer;
    box-sizing: border-box;
}

.tab-bar.app-utilities-bar .tab-button::after {
    content: "";
    position: absolute;
    inset: 0;
    background: linear-gradient(rgb(113, 90, 36), transparent);
    opacity: 0;
    transition: opacity 0.5s ease;
    z-index: -1;
}

.tab-bar.app-utilities-bar .tab-button:hover::after {
    opacity: 1;
}

.tab-bar.app-utilities-bar .tab-button:focus {
    outline: none;
}

.tab-bar.app-utilities-bar .tab-button.btn-danger {
    color: var(--light-red);
    transition: all 0.3s ease;
}

.tab-bar.app-utilities-bar .tab-button.btn-danger::after {
    background: linear-gradient(rgba(150, 40, 27, 0.4), transparent) !important;
}

.tab-bar.app-utilities-bar .tab-button.btn-danger:hover {
    color: #ffffff !important;
}

/* ==========================================================================
   КОНТЕЙНЕР CANVAS И КОРРЕКЦИЯ СИММЕТРИИ
   ========================================================================== */
#canvas-container {
    width: 100vw;
    height: calc(100vh - 165px);
    margin-top: 105px;
    margin-bottom: 60px;
    display: flex;
    justify-content: flex-start;
    align-items: flex-start;
    overflow: auto;
}

canvas {
    display: block;
    touch-action: none;
    background-color: transparent;
    image-rendering: pixelated;
    image-rendering: crisp-edges;
}

/* ==========================================================================
   ОРИГИНАЛЬНЫЙ ТУЛТИП И ФУТЕР
   ========================================================================== */
#calculator-tooltip {
    position: fixed;
    pointer-events: none;
    display: none;
    z-index: 9999;
    width: max-content;
    padding: 15px;
    border-radius: 8px;
    background: linear-gradient(180deg, rgba(20, 20, 24, 0.97) 0%, rgba(11, 11, 13, 0.99) 100%);
    box-shadow: 0 8px 24px rgba(0, 0, 0, 0.7);
    font-family: 'Inter', 'Segoe UI', system-ui, sans-serif;
    box-sizing: border-box;
    white-space: normal;
    letter-spacing: 0.3px;
    opacity: 0;
    transform: translateY(5px);
    transition: opacity 0.12s ease-out, transform 0.12s ease-out;
}

#calculator-tooltip.visible {
    opacity: 1;
    transform: translateY(0);
}

* {
    scrollbar-width: thin;
    scrollbar-color: #f6b36a rgba(0, 0, 0, 0.85);
}

footer {
    background-color: rgba(0, 0, 0, 0.8);
    width: 100%;
    height: 60px;
    display: flex;
    position: fixed;
    bottom: 0;
    left: 0;
    justify-content: center;
    align-items: center;
    padding-top: 10px;
    padding-bottom: 10px;
    box-sizing: border-box;
    z-index: 100;
}

.footer-text {
    display: flex;
    flex-direction: column;
    font-family: 'Oswald', sans-serif;
    font-weight: 400;
    text-align: center;
}

@media (hover: none) and (pointer: coarse) {
    footer {
        width: calc(100% - 20px);
        padding-left: 10px;
        padding-right: 10px;
    }
    .footer-text {
        display: none;
    }
}

footer .header-social-platforms .header-img {
    height: 20px !important;
    width: auto;
    margin-bottom: 10px;
}

footer p a {
    color: white;
    text-decoration: underline;
}

footer p {
    color: white;
    font-size: 14px;
    margin-top: 2px;
    margin-bottom: 2px;
    padding: 0px;
    background-color: transparent;
}

/* ==========================================================================
   ОПТИМИЗИРОВАННАЯ ВЫПАДАЮЩАЯ ПАНЕЛЬ СОХРАНЕНИЙ ВО ВТОРОМ ХЕДЕРЕ
   ========================================================================== */
.profiles-dropdown {
    display: none;
    flex-direction: column;
    background: #111113;
    border: 1px solid #f6b36a; /* Фирменная золотая рамка */
    border-radius: 5px;
    max-height: 260px;
    overflow-y: auto;
    
    /* ИСПРАВЛЕНО: Теперь выпадашка позиционируется абсолютно под вторым хедером */
    position: absolute; 
    top: 45px; /* Четко под нижней границей app-utilities-bar */
    left: 50%;
    transform: translateX(-50%); /* Идеальное центрирование списка по центру экрана */
    width: 320px; /* Фиксированная комфортная ширина списка */
    box-shadow: 0 10px 30px rgba(0, 0, 0, 0.9);
    z-index: 200; /* Гарантированно поверх Canvas и левого меню веток */
}

.profiles-dropdown.open {
    display: flex;
}

.profile-item {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 11px 14px;
    border-bottom: 1px solid rgba(255, 255, 255, 0.04);
    cursor: pointer;
    transition: background 0.2s ease;
}

.profile-item:hover {
    background: rgba(255, 255, 255, 0.08);
}

.profile-item-name {
    color: #ffffff;
    font-family: 'Segoe UI', Arial, sans-serif;
    font-size: 13px;
    overflow: hidden;
    text-overflow: ellipsis;
    white-space: nowrap;
    flex: 1;
}

.profile-item-name.empty {
    color: rgba(255, 255, 255, 0.25);
    font-style: italic;
}

.profile-item-delete {
    color: var(--red);
    font-size: 18px;
    font-weight: bold;
    padding: 0 5px;
    cursor: pointer;
    opacity: 0.6;
    transition: opacity 0.2s ease;
}

.profile-item-delete:hover {
    opacity: 1;
}

/* Модальное окно (остается без изменений) */
.profile-modal-overlay {
    display: none;
    position: fixed;
    inset: 0;
    background: rgba(0, 0, 0, 0.8);
    backdrop-filter: blur(4px);
    justify-content: center;
    align-items: center;
    z-index: 2000;
}

.profile-modal-overlay.open {
    display: flex;
}

.profile-modal-content {
    background: #16161a;
    border: 1px solid #f6b36a;
    border-radius: 8px;
    padding: 20px;
    width: 300px;
    box-shadow: 0 10px 25px rgba(0,0,0,0.5);
}

.profile-modal-content h3 {
    margin: 0 0 15px 0;
    color: #ffffff;
    font-family: 'Oswald', sans-serif;
    font-size: 16px;
    letter-spacing: 1px;
}

.profile-modal-content input {
    width: 100%;
    height: 36px;
    background: #111113;
    border: 1px solid #333;
    border-radius: 4px;
    color: #fff;
    padding: 0 10px;
    box-sizing: border-box;
    margin-bottom: 15px;
}

.profile-modal-content input:focus {
    outline: none;
    border-color: #f6b36a;
}

.modal-actions {
    display: flex;
    justify-content: flex-end;
    gap: 10px;
}

.modal-actions button {
    height: 32px;
    padding: 0 15px;
    border-radius: 4px;
    border: none;
    cursor: pointer;
    font-family: 'Segoe UI', Arial, sans-serif;
    font-size: 12px;
    font-weight: bold;
}

#modal-cancel { background: #333; color: #fff; }
#modal-confirm { background: #f6b36a; color: #000; }

/* ДОБАВЛЕНО: Стилизация инпута, который заменяет имя слота при редактировании */
.profile-item-input {
    flex: 1;
    height: 24px;
    background: #000000;
    border: 1px solid #f6b36a;
    border-radius: 3px;
    color: #ffffff;
    padding: 0 6px;
    font-family: 'Segoe UI', sans-serif;
    font-size: 13px;
    box-sizing: border-box;
    margin-right: 8px;
}

.profile-item-input:focus {
    outline: none;
    box-shadow: 0 0 4px rgba(246, 179, 106, 0.4);
}

/* ДОБАВЛЕНО: Интерактивная RPG-кнопка ОК внутри строки слота */
.profile-item-save-btn {
    background: #f6b36a;
    color: #000000;
    border: none;
    border-radius: 3px;
    height: 24px;
    padding: 0 10px;
    font-family: 'Oswald', sans-serif;
    font-size: 11px;
    font-weight: bold;
    cursor: pointer;
    letter-spacing: 0.5px;
    transition: background 0.2s ease;
    margin-right: 4px;
}

.profile-item-save-btn:hover {
    background: #ffffff;
}

/* ДОБАВЛЕНО: Кнопка отмены редактирования внутри строки слота */
.profile-item-cancel-btn {
    background: rgba(255, 255, 255, 0.05);
    color: #888;
    border: 1px solid #333;
    border-radius: 3px;
    height: 24px;
    padding: 0 8px;
    font-family: 'Segoe UI', sans-serif;
    font-size: 11px;
    cursor: pointer;
    transition: all 0.2s ease;
}

.profile-item-cancel-btn:hover {
    color: var(--red);
    border-color: var(--red);
}

.profiles-dropdown {
    display: none;
    flex-direction: column;
    background: #111113;
    border: 1px solid #f6b36a; 
    border-radius: 5px;
    max-height: 260px;
    overflow-y: auto;
    
    /* ИСПРАВЛЕНО: Позиционируется строго по fixed координатам из JS */
    position: fixed; 
    width: 320px; 
    box-shadow: 0 8px 24px rgba(0, 0, 0, 0.7);
    z-index: 200; 
}
